<%= form_for(@leason) do |f| %>
  <% if @leason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leason.errors.count, "error") %> prohibited this leason from being saved:</h2>

      <ul>
      <% @leason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :class_name %><br>
    <%= f.text_field :class_name %>
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.text_field :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.text_field :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
